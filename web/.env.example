# Copy to `web/.env` and fill in. Nothing reads this file — `dotenvy` loads `.env`.

# Google OAuth client, from https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID="000000000000-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="GOCSPX-xxxxxxxxxxxxxxxxxxxxxxxxxxx"

# The iOS app's own client id, if the phone app is set up. One identity provider
# issues a different client id per platform, so the phone's tokens carry a different
# audience from the browser's and the API has to accept both. See ios/README.md.
# GOOGLE_IOS_CLIENT_ID="000000000000-yyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyy.apps.googleusercontent.com"

# The bundle identifiers of the Apple apps, comma-separated, if they are set up.
#
# Sign in with Apple has no separate client id for a native app: the audience of the
# identity token is the bundle identifier itself, which is why this is a bundle id and
# not something copied out of a console. Leave it unset and the server simply does not
# accept Apple tokens — it still starts, because Apple needs a paid developer account
# and a checkout without one should not fail at boot.
# APPLE_BUNDLE_IDS="com.example.shoppinglist"

# Use an ABSOLUTE path, and mind the three slashes: `sqlite://` + `/Users/...`.
#
# A relative path such as `sqlite:data.db` is resolved against the current working
# directory, and two different things resolve it at two different moments — the sqlx
# query macros at compile time, with cargo's CWD at the workspace root (`web/`), and
# the binary at run time, against whatever directory you launched it from. Those agree
# only as long as you always run cargo from `web/`. An absolute path removes the
# question.
#
# It is machine-specific, which is fine: `.env` is gitignored and this example is what
# gets committed.
#
# First-time setup, from `web/api`:
#     sqlx database create && sqlx migrate run
#
# The binary does not create the database if it is missing — a wrong path fails to
# start rather than silently serving an empty database.
DATABASE_URL="sqlite:///absolute/path/to/repo/web/data.db"

# The origin this application is served from. State-changing requests that say they
# came from anywhere else are refused — see web/src/csrf.rs.
PUBLIC_ORIGIN="http://localhost:8080"

# The session cookie is marked Secure unless this says otherwise, so the safe answer
# is the one you get by not thinking about it. A local run over http:// needs the
# exception; anything reachable from outside must not have it.
SESSION_INSECURE="true"

# Who may sign in. Comma-separated, or "*" to admit anyone.
# Required: without it the server refuses to start, because a default of
# "anyone" would make forgetting to set it look exactly like meaning it.
ALLOWED_EMAILS="you@example.com"

# Android normally needs nothing here. Credential Manager is given the web client id
# above as its serverClientId, and the token comes back addressed to that. The Android
# OAuth client -- registered against the package name and the signing certificate's
# SHA-1 -- is what lets Google attest the app; it does not name the audience.
#
# Set this only if a token does turn up addressed to the Android client.
# GOOGLE_ANDROID_CLIENT_ID="000000000000-zzzzzzzz.apps.googleusercontent.com"

# HTTPS. Unset means TLS_MODE=off: cleartext, which is right behind a proxy that
# terminates TLS and a leak anywhere else. The server says so at every startup.
# See ops/README.md for the two ways to turn it on.
#
# TLS_MODE="acme"                 # or "files", or "off"
# TLS_DOMAINS="list.example.com"  # acme: names that resolve here
# ACME_CONTACT="you@example.com"  # acme: where expiry warnings go
# ACME_DIRECTORY="staging"        # acme: while you fight with port forwarding. Remove after.
# TLS_CACHE_DIR="./tls"           # acme: the account key and certificate. Created 0700.
# TLS_CERT="/etc/ssl/fullchain.pem"  # files: the whole chain, not only the leaf
# TLS_KEY="/etc/ssl/privkey.pem"
# PORT="443"                      # default 8080
# HTTP_REDIRECT_PORT="off"        # default 80 when TLS is on; answers /healthz, redirects the rest

# What the log says, and how loudly.
#
# `error`, `warn`, `info`, `debug`, `trace`, or a list of directives such as
# `api=debug,domain=info`. Unset keeps the filter the server has always used, and
# `RUST_LOG` still works -- where both are set this one wins, and the server says so.
#
# debug and trace log the CONTENTS of people's lists: item names, list names,
# addresses, invite codes. The server warns loudly at startup under either. Everything
# at info and above is free of them by construction -- see docs/configuration.md O2.
# LOG_LEVEL="debug"

# `text` (default) or `json`. JSON is one flattened object per line, for a self-hoster
# who ships the log somewhere that parses it.
# LOG_FORMAT="json"

# Metrics. Off by default: a shopping list for one household does not need to be
# monitored, and a default that opened a port would be a default nobody asked for.
#
# `pull` serves a Prometheus endpoint, `push` exports OTLP to a collector, `both` does
# both -- which is what you have while moving from one to the other.
#
# The scrape endpoint is its OWN listener on its OWN port, bound to loopback. Moving
# that bind to a real interface is refused at startup unless METRICS_TOKEN is set: on a
# home network the endpoint is reachable by the television, and it says how many lists
# exist, how many people signed in, and when the household shops.
#
# METRICS_MODE="pull"
# METRICS_BIND="127.0.0.1"        # anything else requires METRICS_TOKEN
# METRICS_PORT="9100"             # refused if it collides with PORT or HTTP_REDIRECT_PORT
# METRICS_TOKEN=""                # bearer a scraper must present. At least 16 characters
# METRICS_OTLP_ENDPOINT="https://collector.example.com:4318/v1/metrics"  # push and both
# METRICS_OTLP_HEADERS="authorization=Bearer ..."  # split on the first =, so base64 survives
# METRICS_OTLP_INTERVAL_SECONDS="60"
